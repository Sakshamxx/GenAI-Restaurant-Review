# ReviewFlow AI — Production Requirements (CPU-only, optimized)
# Removes CUDA, GPU packages; uses CPU-only PyTorch for lean deployments

# Core ML & Data Processing (CPU-only)
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1
scipy==1.13.1
joblib==1.4.2

# PyTorch CPU-only (significantly smaller than GPU version)
torch==2.2.2 --index-url https://download.pytorch.org/whl/cpu

# Web Framework
fastapi==0.111.0
uvicorn==0.30.1
gunicorn==22.0.0
pydantic==2.8.2

# Request Handling
python-multipart==0.0.9
httpx==0.27.0
PyYAML==6.0.1

# AI & Database
google-genai>=0.1.1
supabase>=2.4.0

# Image & QR
qrcode[pil]>=7.4.2
Pillow>=10.4.0

# Environment & Email
python-dotenv>=1.0.1
resend>=2.0.0

# NLP
nltk==3.8.1

# Utilities
openpyxl==3.1.5
tqdm==4.66.4

# Monitoring & Logging
python-json-logger>=2.0.7
